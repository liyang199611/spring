<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.liyang.mapper.SeckillOrdersMapper">
    <insert id="saveSeckillOrders" parameterType="SeckillOrders">
        insert into seckillOrders(oid,uid,pid,pname,numbers,moner,createtime) values (#{seckillOrders.oid},#{seckillOrders.uid},#{seckillOrders.pid},#{seckillOrders.pname},#{seckillOrders.number},#{seckillOrders.moner},#{seckillOrders.createtime})
    </insert>
</mapper>